# Internal design history

`BRIEF.md` and `DESIGN.md` are the original project brief and design document.
They reference the author's private tooling and ticket IDs and are kept for
history only. Nothing here is required to use or contribute to the package;
the public contract is `src/types.ts`, the root `README.md`, and
`REQUIREMENTS.md`.
