#!/usr/bin/env node
// CLI entry stub. Full implementation (tree view, --json, detect, graph, init)
// lands in Phase 4. This Phase-1 stub proves the bin/shebang/exec packaging
// path end-to-end without carrying any real behavior yet.
//
// Never uses import.meta.url or __dirname (D-04) — this file takes no
// filesystem-relative action at all.

console.log("platform-map: CLI not yet implemented (lands in Phase 4)");
process.exit(0);
